import { BaseApp, Plugin, EnumRequestType, HandleRequest, PluginConfig } from 'jovo-core';
import { Config as AppConfig } from "../App";
export interface Config extends PluginConfig {
    intentMap?: {
        [key: string]: string;
    };
    intentsToSkipUnhandled?: string[];
}
export interface Route {
    path: string;
    type: string;
    intent?: string;
}
export declare class Router implements Plugin {
    config: Config;
    constructor(config?: Config);
    install(app: BaseApp): void;
    uninstall(app: BaseApp): void;
    router(handleRequest: HandleRequest): Promise<void>;
    static intentRoute(handlers: any, state: string | undefined, intent: string, intentsToSkipUnhandled?: string[]): {
        path: string;
        state: string | undefined;
        intent: string;
        type: EnumRequestType;
    };
    /**
     * Returns last level of path
     * @param {string} route
     * @return {string}
     */
    static getLastLevel(route: string): string;
    /**
     * Maps given intent by the platform with a map in the config
     *
     * {
     *     'AMAZON.StopIntent': 'StopIntent',
     * }
     *
     * @param {Config} appConfig
     * @param {string} intentName
     * @returns {string}
     */
    static mapIntentName(appConfig: AppConfig, intentName: string): string;
}
